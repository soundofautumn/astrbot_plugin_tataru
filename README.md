# astrbot_plugin_tataru

TataruBot2 的 AstrBot 插件迁移版本。

当前已迁移的低成本功能：

- `帮帮忙`
- `暖暖`
- `选门`
- `仙人彩`
- `日历`
- `攻略`
- `招募`
- `物品`
- `价格`
- `抽卡`

将 `astrbot_plugin_tataru` 目录放入 AstrBot 的 `data/plugins` 目录后，在 AstrBot WebUI 重载插件即可测试。

## 招募

- `招募 陆行鸟`：默认返回最近 10 条。
- `招募 红玉海`：按具体服务器筛选。
- `招募 战士`：按职业筛选，未指定大区时查询全服。
- `招募 陆行鸟 随机任务`：按分类筛选。
- `招募 陆行鸟 绝龙诗`：无法匹配到分类别名时，会按招募名称和描述关键词搜索。
- `招募 陆行鸟 随机任务 20`：按分类筛选并指定数量，最多 40 条。
- `招募 陆行鸟 高难任务 绝龙诗 20`：可同时指定分类、搜索关键词和数量。
- `招募 陆行鸟 高难度任务`：`高难任务`、`高难度任务`、`高难本` 等别名都会匹配高难任务分类。
- 支持分类：随机任务、迷宫挑战、行会令、讨伐歼灭战、大型任务、高难任务、PvP、金碟、危命任务、寻宝、狩猎、采集、深层迷宫、特殊场景探索、多变迷宫、其他。
- 数据源优先使用 remote-party-finder v2 API，并通过 XIVAPI v2 解析世界名和副本名；失败时自动回退到 v1 API 和网页解析。

## 物品

- `物品 铁矿`：查询物品基础信息和获取方式。
- 物品详情、图标和来源数据直接使用 Garland Tools 国服站；名称搜索使用 XIVAPI v2 辅助匹配物品 ID。

## 价格

- `价格 铁矿`：默认查询四大区最低 10 条挂单。
- `价格 陆行鸟 铁矿`：按大区筛选。
- `价格 红玉海 铁矿 HQ 20`：按服务器筛选，只看 HQ，最多返回 40 条。
- 物价数据使用 Universalis，物品 ID 搜索复用 XIVAPI v2。

## 配置

- `默认使用国际服日历`：关闭时默认查询国服日历，开启后默认查询国际服日历。
- 命令也支持临时指定服务器：`日历 国服`、`日历 国际服`。

后续待迁移：

- `看看微博`
- `房子`
- `输出`
